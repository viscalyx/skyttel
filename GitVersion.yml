# Bump rules follow the approved Kravhantering reference at 562f9d2eccc85a316cf30eaa01a9e4e85c4fbb88.
mode: ContinuousDelivery
tag-prefix: '[vV]'
major-version-bump-message: '\+semver:\s?(breaking|major)|BREAKING CHANGE:'
minor-version-bump-message: '\+semver:\s?(feature|minor)|^feat(\([^)]+\))?!?:'
patch-version-bump-message: '\+semver:\s?(fix|patch)|^(fix|perf)(\([^)]+\))?!?:'
no-bump-message: '\+semver:\s?(none|skip)'
branches:
  main:
    regex: ^main$
    mode: ContinuousDelivery
    label: preview
    increment: Patch
  feature:
    regex: ^(?:f|features?)[/-](?<BranchName>.+)
    mode: ContinuousDelivery
    label: '{BranchName}'
    increment: Minor
    source-branches:
      - main
  fix:
    regex: ^fix[/-](?<BranchName>.+)
    mode: ContinuousDelivery
    label: '{BranchName}'
    increment: Patch
    source-branches:
      - main
  codex:
    regex: ^codex[/-](?<BranchName>.+)
    mode: ContinuousDelivery
    label: '{BranchName}'
    increment: Patch
    source-branches:
      - main
  pull-request:
    regex: ^(pull|pull\-requests|pr)[/-]
    mode: ContinuousDelivery
    label: PullRequest
    increment: Inherit
  tag:
    regex: ^v?\d+\.\d+\.\d+$
    mode: ContinuousDelivery
    label: ''
    increment: None
ignore:
  sha: []
