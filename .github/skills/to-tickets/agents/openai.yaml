interface:
  display_name: "To Tickets"
  short_description: "Split approved plans into linked tickets"
  default_prompt: "Use $to-tickets to split this approved plan or spec into dependency-aware vertical-slice tickets and publish after approval."
policy:
  allow_implicit_invocation: false
