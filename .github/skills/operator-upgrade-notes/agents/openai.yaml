interface:
  display_name: "Operator Upgrade Notes"
  short_description: "Identify PR operator upgrade impacts"
  default_prompt: "Use $operator-upgrade-notes to inspect this branch's diff against main and assess, write or correct required Unreleased guidance."
