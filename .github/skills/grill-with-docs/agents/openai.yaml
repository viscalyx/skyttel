interface:
  display_name: "Grill with Docs"
  short_description: "A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go."
  default_prompt: "Use $grill-with-docs to stress-test this plan and update the glossary or ADRs as decisions are made."
policy:
  allow_implicit_invocation: false
