interface:
  display_name: "Add Override"
  short_description: "Add documented npm package overrides"
  default_prompt: "Use $add-override to add a documented package override in package.json when a dependency needs a forced version."
policy:
  allow_implicit_invocation: false
