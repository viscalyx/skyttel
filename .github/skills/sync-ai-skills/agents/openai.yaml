interface:
  display_name: "Sync AI Skills"
  short_description: "Sync repository skills to AI runtimes"
  default_prompt: "Use $sync-ai-skills to run the sync script, copy .github skills to AI runtime directories, and verify hashes."
policy:
  allow_implicit_invocation: false
