interface:
  display_name: "Report Dead Code"
  short_description: "Find dead code and stale project artifacts"
  default_prompt: "Use $report-dead-code to find removable dead code, stale documentation, and compatibility-only seams, then update an evidence-backed report."
policy:
  allow_implicit_invocation: false
