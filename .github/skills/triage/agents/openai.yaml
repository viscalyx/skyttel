interface:
  display_name: "Triage"
  short_description: "Triage issues and external PRs"
  default_prompt: "Use $triage to triage this issue or external PR and write an agent-ready brief."
policy:
  allow_implicit_invocation: false
