interface:
  display_name: "Run and Fix Integration Tests"
  short_description: "Run and repair Playwright integration specs"
  default_prompt: "Use $run-and-fix-integration-tests to run and repair these failing Playwright integration specs with spec-only edits."
policy:
  allow_implicit_invocation: true
